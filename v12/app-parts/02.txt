'#instrumentName').textContent=`${p.base} / ${state.product==='spot'?'USDT':'USDT 永续'}`;$('#instrumentProduct').textContent=`${p.name} · ${state.product==='spot'?'现货':'永续模拟'}`;$('.coin-logo').textContent=p.icon;$('.coin-logo').style.background=`linear-gradient(145deg,${p.color},color-mix(in srgb,${p.color} 70%,#111))`;$('#lastPrice').textContent=fmt(m.last,p.decimals);$('#lastPriceUsd').textContent=`≈ ${money(m.last)}`;$('#change24').textContent=pct(m.change);$('#change24').className=m.change>=0?'positive':'negative';$('#high24').textContent=$('#mobileHigh').textContent=fmt(m.high,p.decimals);$('#low24').textContent=$('#mobileLow').textContent=fmt(m.low,p.decimals);$('#volume24').textContent=$('#mobileVolume').textContent=compact(m.volumeQuote);$('#chartPairTitle').textContent=`${p.base} / ${state.product==='spot'?'USDT':'USDT 永续'}`;$('#chartWatermark').textContent=`${p.base}USDT`;$('#chartWatermarkSub').textContent=`${state.timeframe} · ${state.product==='spot'?'SPOT':'PERPETUAL'}`;$('#amountUnit').textContent=p.base;$('#orderPrice').value=m.last?m.last.toFixed(p.decimals):'';$('#favoriteButton').classList.toggle('active',state.favorite.has(p.base));updateOrderForm()}
function renderBook(){const p=state.pair,all=[...state.book.asks,...state.book.bids],max=Math.max(1,...all.map(x=>x[1]));const row=(r,side)=>`<div class="book-row three ${side==='bid'?'bid':''}" style="--depth:${Math.max(3,r[1]/max*100)}%"><span class="${side==='bid'?'positive':'negative'}">${fmt(r[0],p.decimals)}</span><span>${fmt(r[1],5)}</span><span>${fmt(r[2],4)}</span></div>`;$('#askList').innerHTML=state.book.asks.slice(0,9).reverse().map(x=>row(x,'ask')).join('');$('#bidList').innerHTML=state.book.bids.slice(0,9).map(x=>row(x,'bid')).join('');const a=state.book.asks[0]?.[0],b=state.book.bids[0]?.[0],m=currentMarket();$('#bookMid').textContent=fmt(m.last,p.decimals);$('#bookSpread').textContent=a&&b?`价差 ${fmt(a-b,p.decimals)}`:'价差 --'}
function renderTrades(){const p=state.pair;$('#tradeList').innerHTML=state.trades.map(t=>`<div class="trade-row three"><span class="${t.side==='buy'?'positive':'negative'}">${fmt(t.price,p.decimals)}</span><span>${fmt(t.qty,5)}</span><span>${new Date(t.ts).toLocaleTimeString('zh-CN',{hour12:false,hour:'2-digit',minute:'2-digit',second:'2-digit'})}</span></div>`).join('')}
function accountSnapshot(){let spot=0,margin=0,unreal=0;for(const pos of Object.values(state.account.positions)){const px=currentMarket(pos.base).last||pos.avg;if(pos.product==='spot'){spot+=pos.qty*px;unreal+=(px-pos.avg)*pos.qty}else{margin+=pos.margin;unreal+=(px-pos.avg)*pos.qty}}const equity=state.account.cash+spot+margin+unreal;return{equity,spot,margin,unreal,cash:state.account.cash,positionValue:spot+margin,ret:(equity-state.account.initial)/state.account.initial*100}}
function accountMetrics(){const s=accountSnapshot(),closed=state.account.trades.filter(x=>x.realized!=null&&x.realized!==0),wins=closed.filter(x=>x.realized>0),losses=closed.filter(x=>x.realized<0),risk=Math.min(100,Math.round(Object.values(state.account.positions).reduce((a,p)=>a+(p.product==='perp'?Math.abs(p.qty)*currentMarket(p.base).last/Math.max(1,s.equity)*24:Math.abs(p.qty)*currentMarket(p.base).last/Math.max(1,s.equity)*12),0)));let peak=state.account.initial,dd=0;for(const x of state.account.equityHistory){peak=Math.max(peak,x.v);dd=Math.min(dd,(x.v-peak)/peak*100)}return{...s,risk,closed,wins,losses,winRate:closed.length?wins.length/closed.length*100:null,drawdown:dd,profitFactor:losses.length?wins.reduce((a,x)=>a+x.realized,0)/Math.abs(losses.reduce((a,x)=>a+x.realized,0)):null}}
function renderAccount(){const s=accountSnapshot();$('#sideEquity').textContent=money(s.equity);$('#availableBalance').textContent=`${fmt(s.cash,2)} USDT`;renderPositions();renderAssets();renderAnalytics()}
function renderPositions(){const rows=Object.values(state.account.positions),orders=state.account.orders,trades=state.account.trades;$('#positionCount').textContent=rows.length;$('#orderCount').textContent=orders.length;let head='',body='';if(state.positionView==='positions'){head='<tr><th>资产</th><th>产品</th><th>方向</th><th>数量</th><th>均价</th><th>现价</th><th>价值/保证金</th><th>未实现盈亏</th><th></th></tr>';body=rows.length?rows.map(pos=>{const p=PAIRS.find(x=>x.base===pos.base),px=currentMarket(pos.base).last||pos.avg,pnl=(px-pos.avg)*pos.qty,value=pos.product==='spot'?px*pos.qty:pos.margin;return`<tr><td><div class="asset-cell"><span class="mini-coin" style="background:${p.color}">${p.icon}</span><span><b>${p.base}</b><small>${p.name}</small></span></div></td><td>${pos.product==='spot'?'现货':`${pos.leverage}x 永续`}</td><td class="${pos.qty>=0?'positive':'negative'}">${pos.product==='spot'?'持有':pos.qty>0?'做多':'做空'}</td><td>${fmt(Math.abs(pos.qty),6)}</td><td>${fmt(pos.avg,p.decimals)}</td><td>${fmt(px,p.decimals)}</td><td>${money(value)}</td><td class="${pnl>=0?'positive':'negative'}">${pnl>=0?'+':''}${money(pnl)}</td><td><button class="row-button" data-close-position="${pos.key}">平仓</button></td></tr>`}).join(''):'<tr class="empty-row"><td colspan="9">暂无持仓。可通过下方买入或卖出按钮进行模拟下单。</td></tr>'}else if(state.positionView==='orders'){head='<tr><th>时间</th><th>交易对</th><th>产品</th><th>方向</th><th>类型</th><th>价格</th><th>数量</th><th></th></tr>';body=orders.length?orders.map(o=>`<tr><td>${new Date(o.createdAt).toLocaleTimeString('zh-CN',{hour12:false})}</td><td>${o.base}/USDT</td><td>${o.product==='spot'?'现货':'永续'}</td><td class="${o.side==='buy'?'positive':'negative'}">${o.side==='buy'?'买入/做多':'卖出/做空'}</td><td>${{limit:'限价',trigger:'条件'}[o.type]||o.type}</td><td>${fmt(o.type==='trigger'?o.trigger:o.price,PAIRS.find(x=>x.base===o.base).decimals)}</td><td>${fmt(o.amount,6)}</td><td><button class="row-button" data-cancel-order="${o.id}">撤单</button></td></tr>`).join(''):'<tr class="empty-row">
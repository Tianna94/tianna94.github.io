<td colspan="8">暂无当前委托</td></tr>'}else{head='<tr><th>时间</th><th>交易对</th><th>产品</th><th>方向</th><th>成交价</th><th>数量</th><th>手续费</th><th>已实现盈亏</th></tr>';body=trades.length?trades.slice(0,80).map(t=>`<tr><td>${new Date(t.filledAt).toLocaleTimeString('zh-CN',{hour12:false})}</td><td>${t.base}/USDT</td><td>${t.product==='spot'?'现货':'永续'}</td><td class="${t.side==='buy'?'positive':'negative'}">${t.side==='buy'?'买入/做多':'卖出/做空'}</td><td>${fmt(t.fillPrice,PAIRS.find(x=>x.base===t.base).decimals)}</td><td>${fmt(t.amount,6)}</td><td>${fmt(t.fee,2)} USDT</td><td class="${(t.realized||0)>=0?'positive':'negative'}">${t.realized?`${t.realized>=0?'+':''}${money(t.realized)}`:'--'}</td></tr>`).join(''):'<tr class="empty-row"><td colspan="8">暂无成交记录</td></tr>'}$('#positionHead').innerHTML=head;$('#positionBody').innerHTML=body;$$('[data-close-position]').forEach(b=>b.onclick=()=>closePosition(b.dataset.closePosition));$$('[data-cancel-order]').forEach(b=>b.onclick=()=>{state.account.orders=state.account.orders.filter(x=>x.id!==b.dataset.cancelOrder);saveAccount();renderAccount();toast('委托已撤销')})}
function executeOrder(o,fill){const total=o.amount*fill,fee=total*(o.product==='spot'?.001:.0005),key=`${o.base}-${o.product}`;let pos=state.account.positions[key],realized=0;if(o.product==='spot'){if(o.side==='buy'){if(state.account.cash<total+fee)return{ok:false,error:'模拟余额不足'};state.account.cash-=total+fee;const old=pos?.qty||0,cost=pos?pos.avg*old:0;state.account.positions[key]={key,base:o.base,product:'spot',qty:old+o.amount,avg:(cost+total)/(old+o.amount)}}else{if(!pos||pos.qty<o.amount)return{ok:false,error:`${o.base} 持仓不足`};realized=(fill-pos.avg)*o.amount-fee;state.account.cash+=total-fee;pos.qty-=o.amount;if(pos.qty<1e-10)delete state.account.positions[key]}}else{const signed=o.side==='buy'?o.amount:-o.amount,lev=o.leverage||10;if(!pos||Math.sign(pos.qty)===Math.sign(signed)){const margin=total/lev;if(state.account.cash<margin+fee)return{ok:false,error:'模拟保证金不足'};state.account.cash-=margin+fee;if(!pos)state.account.positions[key]={key,base:o.base,product:'perp',qty:signed,avg:fill,leverage:lev,margin};else{const a=Math.abs(pos.qty),b=Math.abs(signed);pos.avg=(pos.avg*a+fill*b)/(a+b);pos.qty+=signed;pos.margin+=margin}}else{const close=Math.min(Math.abs(pos.qty),Math.abs(signed)),dir=Math.sign(pos.qty),release=pos.margin*close/Math.abs(pos.qty);realized=(fill-pos.avg)*close*dir-fee;state.account.cash+=release+realized;pos.margin-=release;pos.qty-=dir*close;if(Math.abs(pos.qty)<1e-10)delete state.account.positions[key]}}state.account.fees+=fee;state.account.trades.unshift({...o,fillPrice:fill,fee,realized,filledAt:Date.now()});const s=accountSnapshot();state.account.equityHistory.push({t:Date.now(),v:s.equity});state.account.equityHistory=state.account.equityHistory.slice(-100);saveAccount();renderAccount();return{ok:true}}
function placeOrder(){const m=currentMarket(),amount=+$('#orderAmount').value,price=+$('#orderPrice').value||m.last,trigger=+$('#triggerPrice').value,leverage=+$('#leverage').value.replace('x','')||10;if(!amount||amount<=0)return toast('请输入有效数量');if(state.orderType==='trigger'&&!trigger)return toast('请输入触发价格');const o={id:uid(),base:state.pair.base,product:state.product,type:state.orderType,side:state.side,amount,price,trigger,leverage,createdAt:Date.now()};if(state.orderType==='market'){const r=executeOrder(o,m.last);toast(r.ok?'模拟成交成功':r.error)}else{state.account.orders.unshift(o);saveAccount();renderAccount();toast(state.orderType==='trigger'?'条件委托已创建':'限价委托已创建')}$('#orderAmount').value='';updateOrderForm()}
function checkOrders(){const px=currentMarket().last;for(const o of [...state.account.orders]){if(o.base!==state.pair.base)continue;const hit=o.type==='limit'?(o.side==='buy'?px<=o.price:px>=o.price):(o.side==='buy'?px>=o.trigger:px<=o.trigger);if(hit){const r=executeOrder(o,o.type==='limit'?o.price:px);if(r.ok)state.account.orders=state.account.orders.filter(x=>x.id!==o.id)}}saveAccount()}
function closePosition(key){const pos=state.account.positions[key];if(!pos)return;const r=executeOrder({id:uid(),base:pos.base,product:pos.product,type:'market',side:pos.product==='spot'?'sell':pos.qty>0?'sell':'buy',amount:Math.abs(pos.qty),leverage:pos.leverage||10,createdAt:Date.now()},currentMarket(pos.base).last||pos.avg);toast(r.ok?'模拟平仓完成':r.error)}
function updateOrderForm(){const amount=+$('#orderAmount').value||0,price=state.orderType==='market'?currentMarket().last:(+$('#orderPrice').value||currentMarket().last),value=amount*price,fee=value*(state.product==='spot'?.001:.0005);$('#priceField').classList.toggle('hidden',state.orderType==='market');$('#triggerField').classList.toggle('hidden',state.orderType!=='trigger');$('#leverageField').classList.toggle('hidden',state.product!=='perp');$('#orderValue').textContent=`${fmt(value,2)} USDT`;$('#orderFee').textContent=`${fmt(fee,2)} USDT`;const btn=$('#submitOrder'),action=state.side==='buy'?(state.product==='spot'?'买入':'做多'):(state.product==='spot'?'卖出':'做空');btn.textContent=`模拟${action} ${state.pair.base}`;btn.className=`submit-order ${state.side}`}
function renderMarketPage(){const list=PAIRS.filter(p=>(p.base+p.name).toLowerCase().includes(($('#marketSearch').value||'').toLowerCase()));if(state.marketFilter==='gainers')list.sort((a,b)=>currentMarket(b.base).change-currentMarket(a.base).change);if(state.marketFilter==='volume')list.sort((a,b)=>currentMarket(b.base).volumeQuote-currentMarket(a.base).volumeQuote);const all=PAIRS.map(p=>currentMarket(p.base)),up=all.filter(x=>x.change>=0).length,volume=all.reduce((a,x)=>a+x.volumeQuote,0),avg=all.reduce((a,x)=>a+Math.abs(x.change),0)/all.length;$('#marketSummary').innerHTML=[["主流资产成交额",compact(volume),"24小时"],["上涨资产",`${up} / ${PAIRS.length}`,"实时统计"],["平均波动",`${avg.toFixed(2)}%`,"绝对涨跌"],["数据